{
  "name": "masterchef-v2",
  "description": "NobleSwap MasterChef V2 Subgraph",
  "version": "1.0.0",
  "repository": "git@github.com:gauss-gang/nobleswap-subgraph.git",
  "author": "NobleSwap",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "create-local": "graph create --node http://localhost:8020 nobleswap/masterchef-v2",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 nobleswap/masterchef-v2",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pancakeswap/masterchef-v2 subgraph.yaml"
  }
}
